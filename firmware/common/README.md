# Shared definitions

Protocol and message definitions used by both the tag and the bay station --
frame formats, tag IDs, and whatever payload rides on the ranging exchange.

Empty. Keep this the single source of truth rather than duplicating struct
definitions on both sides.
