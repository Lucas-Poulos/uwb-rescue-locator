# Wristband-only libraries

Symbols/footprints used **only** on the wristband board. If a part is also
used on the bay station (e.g. the UWB IC once chosen), it belongs in
`../../shared/` instead.

Empty for now -- populate as parts get chosen (see `../../docs/decisions.md`).
